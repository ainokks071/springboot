<!DOCTYPE html>
<html lang="en">
<head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
            {{>layouts/header}}

<div class="container mt-3">
    <h1>Hello, mustache!</h1>
    <div class="card">
        <div class="card-header"> Mustache </div>
        <div class="card-body">

            <!--contents-->
            <p>View template(화면 담당) : html과 같은 웹페이지 골격에 변수를 활용</p>
            <p>mustache는 뷰 템플릿의 한 종류!</p>
            <p>localhost:8080/hello와 같은 "요청"을 controller에서 받아, Model을 거쳐, 포워딩을 통해 클라이언트에게 View로 "응답"한다.</p>
            <p>view는 controller(처리담당), model(데이터담당) 필요하겠지.</p>
            <p>{{username}}님, 반갑습니다 !</p>

        </div>
        <div class="card-footer">Footer</div>
    </div>
</div>

</body>
</html>
