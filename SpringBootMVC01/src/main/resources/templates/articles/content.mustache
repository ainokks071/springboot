<!DOCTYPE html>
<html lang="en">
<head>
    <title>Bootstrap Example</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>

    <script>
        function changeReadonly() {
            $('#title').attr('readonly', false)
            $('#content').attr('readonly', false)
        }
    </script>

    <style>
        table {
          font-family: arial, sans-serif;
          border-collapse: collapse;
          width: 100%;
        }

        th {
          border: 1px solid #dddddd;
          text-align: center;
          padding: 8px;
        }

        td {
           border: 1px solid #dddddd;
           text-align: left;
           padding: 8px;
        }
    </style>
</head>
<body>

{{>layouts/header}}

<div class="container mt-3">
    <h1>게시판 상세페이지</h1>
    <div class="card">
        <div class="card-header"> 게시판 상세보기 </div>
        {{#article}}
            <div class="card-body">
                <table border="2">
                    <thead>
                    <tr>
                        <th>Num</th>
                        <th>Title</th>
                        <th>Content</th>
                    </tr>
                    </thead>
                    <tbody>
                    {{#article}}
                        <tr>
                            <td>{{id}}</td>
                            <td>{{title}}</a></td>
                            <td>{{content}}</td>
                        </tr>
                    {{/article}}
                    </tbody>
                </table>
                    <br>
                    <button type="submit" class="btn btn-primary btn-sm" onclick="location.href='/articles/edit/{{article.id}}'">수정화면</button>
                    <button type="button" class="btn btn-success btn-sm" onclick="location.href='/articles/list'">목록으로</button>
                    <button type="button" class="btn btn-danger btn-sm" onclick="location.href='/articles/delete/{{article.id}}'">삭제하기</button>
            </div>
        {{/article}}
        <div class="card-footer">ainokks071@gmail.com</div>
    </div>
</div>

</body>
</html>

